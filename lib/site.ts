/** Canonical production origin (custom domain on GitHub Pages). */
export const SITE_URL = "https://reframestudio.es";

/** Default locale — must match `proxy.ts` / `lib/i18n.ts` / `hreflang` x-default. */
export const DEFAULT_LOCALE = "es";

export const SEO_LOCALES = ["es", "en", "fr"] as const;
